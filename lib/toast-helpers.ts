// Toast helpers — no-ops. Toasts have been removed from the UI.
// Kept as stubs to avoid breaking any remaining imports during migration.

export function toastSuccess(_toast: unknown, _t: unknown, _options?: unknown) {}
export function toastInfo(_toast: unknown, _t: unknown, _options?: unknown) {}
