#!/usr/bin/env bash
# Intentionally left as a no-op.
# The autolog/version bump runs in commit-msg so it can read the current
# commit message instead of the previous .git/COMMIT_EDITMSG contents.
exit 0

